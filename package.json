{
  "name": "orbe-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:client": "cd apps/client && bun run dev",
    "dev:api": "cd apps/api && bun run dev",
    "dev:desktop": "cd apps/desktop && bunx tauri dev",
    "dev:android": "cd apps/desktop && bunx tauri android dev",
    "dev:ios": "cd apps/desktop && bunx tauri ios dev",
    "dev": "bunx concurrently \"bun run dev:api\" \"bun run dev:desktop\""
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/cli": "^2.11.4"
  }
}
